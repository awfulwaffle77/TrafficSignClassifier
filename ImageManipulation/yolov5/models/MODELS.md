`best-fp16.tflite` is the first model trained, with 128x128 images with
a 16 batch, 3 epochs

`best.pt` is the file containing the PyTorch weights

`best.onnx` is in the universal onnx format

`customyolov5 is the TF Graph
